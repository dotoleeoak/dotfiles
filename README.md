# My dotfiles

Prerequisites: Nix, Home Manager

```sh
mkdir -p ~/.config
git clone https://github.com/dotoleeoak/dotfiles.git ~/.config/home-manager
cd ~/.config/home-manager
home-manager switch -b backup
```
